{
    "name": "webhook-platform/php",
    "description": "Official PHP SDK for Hookflow \u2014 reliable webhook infrastructure",
    "type": "library",
    "license": "MIT",
    "keywords": [
        "webhook",
        "webhooks",
        "api",
        "events",
        "delivery"
    ],
    "authors": [
        {
            "name": "Vadym Kykalo",
            "email": "vadymkykalo@gmail.com"
        }
    ],
    "require": {
        "php": ">=8.1",
        "ext-json": "*",
        "ext-curl": "*"
    },
    "require-dev": {
        "phpunit/phpunit": "^10.0",
        "phpstan/phpstan": "^1.10"
    },
    "autoload": {
        "psr-4": {
            "Hookflow\\": "src/"
        }
    },
    "autoload-dev": {
        "psr-4": {
            "Hookflow\\Tests\\": "tests/"
        }
    },
    "scripts": {
        "test": "phpunit",
        "test:contract": "phpunit -c phpunit.contract.xml",
        "analyse": "phpstan analyse src"
    },
    "config": {
        "sort-packages": true
    }
}
